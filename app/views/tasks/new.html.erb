<h1>タスク新規作成ページ　--New task create--</h1>

<%= render 'form', task: @task %>

<%
=begin
%>

<%= form_with(model: @task, local:true) do |f| %>

<%= f.label :content, 'タスク' %>

<%= f.text_field :content %>

<%= f.submit '新規登録' %>

<% end %>

<%
=end
%>



<%= link_to '一覧に戻る', tasks_path %>
