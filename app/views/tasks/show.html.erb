<h1>id:<%= @task.id %> のタスク詳細ページ</h1>

<p>ユーザーＩＤ：<%= @task.user_id %></p>
<p>タスク：<%= @task.content %></p>
<p>ステータス：<%= @task.status %></p>

<%= link_to '一覧に戻る', root_url %>
<%= link_to 'このタスクを編集する', edit_task_path(@task) %>
<%= link_to 'このタスクを削除する', @task, method: :delete, data: { confirm: '削除してよろしいですか？' } %>
