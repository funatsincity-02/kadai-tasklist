
<% if logged_in? %>

<h1>タスク一覧</h1>


<% if @tasks.any? %>

      <% @tasks.each do |task| %>
      
        <%= link_to task.id, task %>　：　<%= task.content %>　＞　<%= task.status %>
      
      <% end %>

  <div>

  </div>
  <% end %>
  <%= link_to '新規タスクの投稿', new_task_path %>

<% else %>

    <h1>タスクリスト</h1>      
    <%= link_to 'ユーザー登録!', signup_path %>


<% end %>
  

